import { withStyles } from "@/utils";

import * as styles from "./styles";

export const PageSection = withStyles("section", styles.wrapper);

export const PageTags = withStyles("div", styles.tags);

export const PageTab = withStyles("div", styles.tab);
