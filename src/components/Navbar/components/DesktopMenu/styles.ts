import { styled } from "@/utils";

export const wrapper = styled(`
  hidden lg:flex
  items-center
  gap-12
`);

export const link = styled(`
  outline-none
  text-white
  text-sm
  font-normal
  tracking-wider
`);
